// Feature flags. Flip a value here to enable/disable a whole surface.
// Typed as `boolean` (not the literal) so guards don't trigger no-unreachable.

/** Shop/catalog is temporarily disabled — the tab shows "Soon" and routes show a coming-soon screen. */
export const SHOP_ENABLED: boolean = false;
